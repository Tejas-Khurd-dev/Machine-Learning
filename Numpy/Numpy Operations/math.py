import numpy as np

arr = np.array([10, 20, 30, 40, 50])
print(arr + 5)
print(arr * 2)
# In NumPy, the / operator always performs true division (floating-point division), even if both operands are integers.
print(arr / 5)
print(arr ** 2)
print(arr // 5) # --> Floor division

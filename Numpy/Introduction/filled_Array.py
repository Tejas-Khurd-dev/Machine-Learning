import numpy as np

# array_name = np.full(shape, value) --> for 1d array
# array_name = np.full((shape), value) --> for multidimensional array
filled_array = np.full((3,3), 11)
print(filled_array)